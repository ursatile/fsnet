@model IEnumerable<Artist>
@{
    ViewData["Title"] = "Home Page";
}

<h1>Artists</h1>
<ul>
    @foreach (var artist in Model) {
        <li>
            <a href="@Url.Action("View", "Artists", new { artist.Id })">@artist.Name</a>
        </li>
    }
</ul>